ENCITA V12 PERFORMANCE + RESPONSIVE ADMIN

Changes from V11:
- Mobile performance pass: keeps a lightweight branded animated background but disables expensive continuous effects.
- Mobile hero uses only gentle logo float/aura; orbit particles/sparks/hearts are disabled on small screens.
- Service marquee becomes touch-scrollable on mobile.
- Blur-heavy reveal/background effects removed on mobile.
- Below-the-fold sections use content-visibility where supported.
- Scroll-progress work is skipped on mobile.
- Analytics page-view write is deferred until idle.
- Appointment inputs remain 16px+ on mobile to avoid iOS zoom.
- Admin CMS is responsive on tablets and phones: sticky horizontal nav, stacked forms/items, touch-sized controls, scrollable tables, responsive metrics.

SUPABASE: No SQL changes are required for V12. Keep the V11 migration already installed.
